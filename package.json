{
  "name": "bitkong",
  "private": true,
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "npm ci --prefix server --omit=dev && npm ci --prefix web && npm run build --prefix web",
    "start": "npm start --prefix server"
  }
}
